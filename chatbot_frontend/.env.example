# PUBLIC_INTERFACE
# Example environment variables for configuring the frontend API base URL.
# If not set, the development server will rely on package.json "proxy" to forward /api requests.
REACT_APP_API_BASE_URL=http://localhost:8000
