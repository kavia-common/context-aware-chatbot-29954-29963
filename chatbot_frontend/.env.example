# Create React App development overrides for preview environments
# Allows dev server to bind to all interfaces and accept forwarded hosts.

HOST=0.0.0.0
PORT=3000

# CRA maps this to webpack-dev-server allowedHosts/host check behavior in development.
# Intended ONLY for local/CI preview usage. Do not use on public internet.
DANGEROUSLY_DISABLE_HOST_CHECK=true

# Optional: backend URL if not using CRA proxy
# REACT_APP_API_BASE_URL=http://localhost:8000
